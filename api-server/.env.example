# 服务方服务器配置；买家公众号凭证不在此保存。
PORT=18890
API_TOKEN=replace-with-service-access-token
WECHAT_CREDENTIAL_MODE=request
# 入口必须 HTTPS，禁止反代/APM记录敏感请求头、正文或含token的上游URL。
# 旧自用服务可显式设置legacy并沿用原WECHAT_APP_ID/WECHAT_APP_SECRET；不能交付为买家入口。
